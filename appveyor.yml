os: Visual Studio 2015

build_script:
  - cmd: msbuild /t:rebuild /v:minimal TddStud10.VSCode.sln
  - cmd: .\build.cmd BuildPackage

test: off

artifacts:
  - path: temp\*.vsix
    name: TddStud10 VSCode Extension